{
  "name": "tour-of-heroes-server",
  "version": "1.0.0",
  "description": "fake /heroes server for tour-of-heroes",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "json-server": "^0.17.0"
  }
}
